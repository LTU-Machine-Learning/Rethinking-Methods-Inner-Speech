Following four folders have been sahred for the code:

1. Raw_data

This folder contains the two sub-folders which corresponds to original data of word and vowels. In each subfolder, there are 15 files, each for one subject.

2. Preprocessing:

This folder contains, files and functions which were used during the preprocessing of both vowel and words. Here, folders which having processed data with and without downsanling have also been shared.
Foldet; 'picard_master' shared to run ICA.

3.Training_and_functions:

This folder contains the training scripts for both vowel and words. Dependent functiona are also been been in same folder.

4. Testing_TrainedModels:

This folder contains testing script along with trained models.


***The comments are available in the script to follow the process. The script conatins; data parameters, CNN parameters, CNN architecture, data split, reshape, and CNN training options and training.